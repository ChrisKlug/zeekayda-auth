@page
@{
    ViewData["Title"] = "Home";
}
<h1>ZeeKayDa sample identity server</h1>
<p>
    This server has no pages of its own to visit: a client application starts a sign-in at the
    authorization endpoint, and the framework brings the browser to the login page.
</p>
<p>Discovery: <a href="/.well-known/openid-configuration">/.well-known/openid-configuration</a></p>
