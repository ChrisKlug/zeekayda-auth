# ADR 0015 — Signing-Key Provider: KeySet / KeySource Tiers and the Data-Not-Objects Contract

**Retired.** This ADR's content has been merged into [ADR 0011](./0011-signing-key-management.md) — read
it for the current `KeySetOptions`/`KeySourceOptions` provider contract and the security sign-off that
governed this reshape (recorded there as "Round 2"). This ADR number is retired and must not be reused.
