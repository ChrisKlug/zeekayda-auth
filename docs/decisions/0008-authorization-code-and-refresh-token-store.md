# ADR 0008 — Authorization Code and Refresh Token Store

**Retired.** This ADR's authorization-code-store content, and the store rules shared with the
refresh-token store (registration policy, options placement, exception contract), have been merged
into [ADR 0013](./0013-store-protocol-persistence-split.md). The refresh-token-store reshape lives in
[ADR 0014](./0014-refresh-token-grant-store.md). This ADR number is retired and must not be reused.
